# grok-build ZDR documentation

This directory contains the VitePress documentation site for the ZDR-enhanced
grok-build harness.

```bash
npm install
npm run docs:dev
```

Build the production site with:

```bash
npm run docs:build
```

The generated site is written to `.vitepress/dist/`.
